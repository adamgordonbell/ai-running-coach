[project]
name = "ai-running-coach"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0",
    "boto3",
    "httpx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
